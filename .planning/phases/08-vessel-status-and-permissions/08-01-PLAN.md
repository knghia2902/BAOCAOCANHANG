---
phase: 08-vessel-status-and-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/excel/WeighbridgeService.ts
  - tests/WeighbridgeService.spec.ts
autonomous: true
requirements:
  - VESSEL-01

must_haves:
  truths:
    - "Vessel interface includes status?: 'in_progress' | 'done'"
    - "getVessels and getLocalVessels default undefined status to 'in_progress'"
    - "createVessel assigns status: 'in_progress'"
    - "updateVesselStatus updates local IndexedDB cache and syncs to Supabase weighbridge_vessels"
    - "Unit test verifies status defaulting, update status, and error resilience"
  artifacts:
    - path: "src/services/excel/WeighbridgeService.ts"
      provides: "Vessel interface with status and updateVesselStatus method"
      contains: "updateVesselStatus"
    - path: "tests/WeighbridgeService.spec.ts"
      provides: "Vitest unit tests for vessel status lifecycle"
      contains: "updateVesselStatus"
---

<objective>
Cập nhật cấu trúc dữ liệu `Vessel` trong `WeighbridgeService.ts` để bổ sung trường `status: 'in_progress' | 'done'` (mặc định `'in_progress'`), triển khai hàm `updateVesselStatus`, đồng bộ Supabase & IndexedDB, và viết bộ unit test kiểm thử tự động.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-vessel-status-and-permissions/08-CONTEXT.md
@.planning/phases/08-vessel-status-and-permissions/08-RESEARCH.md
@.planning/phases/08-vessel-status-and-permissions/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Vessel interface and query fallbacks in WeighbridgeService</name>
  <files>src/services/excel/WeighbridgeService.ts</files>
  <action>
    - Trong `interface Vessel`: bổ sung thuộc tính `status?: 'in_progress' | 'done';`.
    - Trong `getVessels()`: map dữ liệu từ Supabase hoặc local cache gán `status: vessel.status || 'in_progress'`.
    - Trong `createVessel(name: string)`: gán `status: 'in_progress'` cho đối tượng `newVessel` và đưa `status: 'in_progress'` vào payload insert Supabase.
  </action>
  <verify>
    <automated>npx vue-tsc -b --noEmit</automated>
  </verify>
  <done>TypeScript biên dịch thành công, trường status được tích hợp đầy đủ vào model Vessel.</done>
</task>

<task type="auto">
  <name>Task 2: Implement updateVesselStatus in WeighbridgeService</name>
  <files>src/services/excel/WeighbridgeService.ts</files>
  <action>
    - Triển khai phương thức `async updateVesselStatus(id: number, status: 'in_progress' | 'done'): Promise<boolean>` trong `WeighbridgeService`.
    - Cập nhật trường `status` của tàu tương ứng trong IndexedDB cache (`getLocalVessels`, `saveLocalVessels`).
    - Gọi Supabase `.from('weighbridge_vessels').update({ status }).eq('id', id)`.
    - Xử lý lỗi: Nếu Supabase gặp lỗi mạng hoặc chạy offline, vẫn giữ cập nhật local IndexedDB và trả về `true` (kèm `console.warn`).
  </action>
  <verify>
    <automated>npx vue-tsc -b --noEmit</automated>
  </verify>
  <done>Phương thức updateVesselStatus hoạt động trơn tru với cả Supabase và IndexedDB cache.</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for WeighbridgeService status lifecycle</name>
  <files>tests/WeighbridgeService.spec.ts</files>
  <action>
    - Tạo tệp kiểm thử `tests/WeighbridgeService.spec.ts` sử dụng Vitest.
    - Mock `@/supabase` và `@/services/storage/DBContext`.
    - Viết các test cases:
      1. Default status: `getVessels()` tự động gán `status: 'in_progress'` cho các bản ghi cũ chưa có status.
      2. Create vessel: `createVessel('Tàu Mới')` tạo bản ghi có `status: 'in_progress'`.
      3. Update status to 'done': `updateVesselStatus(id, 'done')` cập nhật cache local và gọi Supabase update `{ status: 'done' }`.
      4. Update status to 'in_progress': `updateVesselStatus(id, 'in_progress')` cập nhật cache local và gọi Supabase update `{ status: 'in_progress' }`.
      5. Error resilience: Khi Supabase update lỗi, hàm không throw exception và cache local vẫn được bảo toàn.
  </action>
  <verify>
    <automated>npm test -- tests/WeighbridgeService.spec.ts</automated>
  </verify>
  <done>Toàn bộ 5 test cases vượt qua thành công.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Dữ liệu trạng thái tàu được lưu trữ tại Supabase và IndexedDB local cache.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | updateVesselStatus | mitigate | Ràng buộc kiểu dữ liệu TypeScript chỉ chấp nhận `'in_progress' \| 'done'`. |
| T-08-01-02 | Denial of Service | Offline sync | mitigate | Cơ chế lưu cache IndexedDB đảm bảo ứng dụng hoạt động bình thường kể cả khi mất mạng. |
</threat_model>

<verification>
Chạy `npm test -- tests/WeighbridgeService.spec.ts` và `npx vue-tsc -b --noEmit`.
</verification>

<success_criteria>
Cấu trúc dữ liệu Vessel và phương thức updateVesselStatus được xây dựng hoàn thiện và vượt qua toàn bộ unit test.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/08-vessel-status-and-permissions/08-01-SUMMARY.md`.
</output>
