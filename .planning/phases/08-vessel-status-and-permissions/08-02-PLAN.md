---
phase: 08-vessel-status-and-permissions
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/components/tools/WeighbridgePrinter.vue
autonomous: true
requirements:
  - VESSEL-01
  - VESSEL-02

must_haves:
  truths:
    - "Sidebar and Global Dashboard display 2 synchronized tabs: Đang làm hàng and Đã xong with count badges"
    - "Vessel summary header displays status badge: Đang làm hàng or Đã xong"
    - "Close vessel button triggers confirmation warning modal"
    - "Marking vessel as done sets read-only mode for vessel, barges, and tickets"
    - "Reopen vessel button is only visible and accessible for Admin users"
  artifacts:
    - path: "src/components/tools/WeighbridgePrinter.vue"
      provides: "2-Tab UI, Vessel closing flow, Read-only gating, and Admin reopen capability"
      contains: "selectedVesselStatusTab"
---

<objective>
Tích hợp giao diện 2 tab chuyển đổi ("Đang làm hàng" và "Đã xong") tại Sidebar và Báo cáo tổng quan, triển khai nút chốt tàu kèm modal cảnh báo xác nhận, kích hoạt chế độ Chỉ xem (Read-only) khi tàu đã xong, và bổ sung nút mở lại tàu phân quyền riêng cho Admin trong `WeighbridgePrinter.vue`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-vessel-status-and-permissions/08-CONTEXT.md
@.planning/phases/08-vessel-status-and-permissions/08-RESEARCH.md
@.planning/phases/08-vessel-status-and-permissions/08-VALIDATION.md
@.planning/phases/08-vessel-status-and-permissions/08-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tab state and computed filters in WeighbridgePrinter.vue</name>
  <files>src/components/tools/WeighbridgePrinter.vue</files>
  <action>
    - Khai báo reactive state: `selectedVesselStatusTab = ref<'in_progress' | 'done'>('in_progress');`.
    - Tạo các computed:
      - `inProgressVessels = computed(() => vessels.value.filter(v => (v.status || 'in_progress') === 'in_progress'))`
      - `doneVessels = computed(() => vessels.value.filter(v => v.status === 'done'))`
      - `filteredSidebarVessels = computed(() => selectedVesselStatusTab.value === 'done' ? doneVessels.value : inProgressVessels.value)`
      - `isCurrentVesselDone = computed(() => activeVessel.value?.status === 'done')`
    - Cập nhật computed `allBarges`: Lọc theo các tàu thuộc `selectedVesselStatusTab` để Báo cáo tổng quan hiển thị đúng sà lan của tab tương ứng.
  </action>
  <verify>
    <automated>npx vue-tsc -b --noEmit</automated>
  </verify>
  <done>Bộ lọc tab và computed state được thiết lập đồng bộ, không có lỗi TypeScript.</done>
</task>

<task type="auto">
  <name>Task 2: Build 2-Tab navigation in Sidebar and Global Dashboard</name>
  <files>src/components/tools/WeighbridgePrinter.vue</files>
  <action>
    - Tại Sidebar (dưới tiêu đề "Tổng quan"): Thêm 2 nút tab dạng pill:
      - Tab "Đang làm hàng": badge hiển thị `({{ inProgressVessels.length }})`.
      - Tab "Đã xong": badge hiển thị `({{ doneVessels.length }})`.
      - V-for danh sách tàu đổi sang lặp qua `filteredSidebarVessels`.
    - Tại Báo cáo tổng quan (Global Dashboard):
      - Thêm 2 tab chuyển đổi đồng bộ với `selectedVesselStatusTab`.
      - Các thẻ thống kê (Tổng số tàu, Tổng số sà lan) cập nhật số liệu theo tab đang chọn.
  </action>
  <verify>
    <automated>npx vue-tsc -b --noEmit</automated>
  </verify>
  <done>2 Tab hiển thị rõ ràng tại cả Sidebar và Dashboard, đồng bộ mượt mà khi chuyển đổi.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Close Vessel & Admin Re-Open flows with confirmation modals</name>
  <files>src/components/tools/WeighbridgePrinter.vue</files>
  <action>
    - Viết hàm `handleCloseVessel(vesselId: number)`:
      - Sử dụng `showConfirm` hiển thị cảnh báo: *"Bạn có chắc chắn muốn chốt số liệu tàu này? Sau khi chốt, tàu sẽ chuyển sang chế độ Chỉ xem và chỉ tài khoản Admin mới có quyền mở lại."*
      - Gọi `WeighbridgeService.updateVesselStatus(vesselId, 'done')`.
      - Cập nhật reactive state trong `vessels.value`, chuyển `selectedVesselStatusTab = 'done'`, hiển thị toast thành công.
    - Viết hàm `handleReopenVessel(vesselId: number)`:
      - Kiểm tra bảo vệ `if (authStore.role !== 'admin') return;`.
      - Sử dụng `showConfirm` xác nhận: *"Xác nhận mở lại tàu để tiếp tục cập nhật số liệu?"*.
      - Gọi `WeighbridgeService.updateVesselStatus(vesselId, 'in_progress')`.
      - Cập nhật reactive state, chuyển `selectedVesselStatusTab = 'in_progress'`, hiển thị toast thành công.
    - Tại tiêu đề Báo cáo tổng hợp tàu (Vessel Summary Header):
      - Hiển thị badge trạng thái ("Đang làm hàng" màu teal / "Đã xong" màu xám).
      - Khi `status === 'in_progress'`: Hiện nút *"Chốt số liệu: Đã xong"* (nền amber/cam).
      - Khi `status === 'done'` và `authStore.role === 'admin'`: Hiện nút *"Mở lại: Đang làm hàng"* (nền primary/indigo).
  </action>
  <verify>
    <automated>npx vue-tsc -b --noEmit</automated>
  </verify>
  <done>Quy trình chốt tàu và mở lại hoạt động chuẩn xác với phân quyền Admin nghiêm ngặt.</done>
</task>

<task type="auto">
  <name>Task 4: Enforce Read-Only mode for completed vessels</name>
  <files>src/components/tools/WeighbridgePrinter.vue</files>
  <action>
    - Khi `isCurrentVesselDone` là true:
      - Tại Vessel Summary Dashboard: Hiển thị banner cảnh báo: *"Tàu đã chốt số liệu — Chế độ chỉ xem"* với icon `lock`.
      - Ẩn hoặc vô hiệu hóa các nút: Thêm sà lan mới, Đổi tên tàu, Xóa tàu.
      - Tại khu vực Active Barge:
        - Hiển thị banner cảnh báo chế độ chỉ xem.
        - Ẩn hoặc vô hiệu hóa: Thêm xe, Sửa xe, Xóa xe, Nhập Excel, Đồng bộ từ phân bổ, Xóa tất cả xe, Toggle khóa sà lan.
        - Giữ nguyên hoạt động: In phiếu lẻ, In hàng loạt A5, Xuất Excel báo cáo.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Chế độ Chỉ xem được áp dụng toàn diện, bảo vệ an toàn tuyệt đối cho số liệu đã chốt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phân quyền Admin khi mở lại tàu và thao tác dữ liệu.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Elevation of Privilege | Reopen Vessel Button | mitigate | Kiểm tra chặt chẽ `authStore.role === 'admin'` cả ở giao diện v-if lẫn trong thân hàm `handleReopenVessel`. |
| T-08-02-02 | Tampering | Read-only Bypass | mitigate | Vô hiệu hóa và ẩn tất cả các nút chỉnh sửa/xóa khi `isCurrentVesselDone` là true. |
</threat_model>

<verification>
Chạy `npm run build` để đảm bảo toàn bộ ứng dụng đóng gói không có lỗi cú pháp hoặc template binding.
</verification>

<success_criteria>
Giao diện 2 Tab, quy trình chốt tàu, chế độ Read-only và quyền Admin mở lại hoạt động trơn tru trên `WeighbridgePrinter.vue`.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/08-vessel-status-and-permissions/08-02-SUMMARY.md`.
</output>
