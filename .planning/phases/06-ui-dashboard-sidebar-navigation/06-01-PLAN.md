---
phase: 06-ui-dashboard-sidebar-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tools/CargoAllocator.vue
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "D-01: Navigation item for Lịch sử cân Kho & Container is added to left sidebar"
    - "activeSubViewMode supports 'other_tickets' value"
    - "WeighbridgeOtherManager component stub is imported and conditionally rendered"
  artifacts:
    - path: "src/components/tools/CargoAllocator.vue"
      provides: "Sidebar navigation entry and layout switcher"
      contains: "activeSubViewMode === 'other_tickets'"
---

<objective>
Tích hợp mục điều hướng mới "Lịch sử cân Kho & Container" (D-01) vào thanh sidebar bên trái của tệp `CargoAllocator.vue` và thiết lập kết xuất điều kiện cho component tra cứu mới.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-dashboard-sidebar-navigation/06-CONTEXT.md
@.planning/phases/06-ui-dashboard-sidebar-navigation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sidebar nav link and active state</name>
  <files>src/components/tools/CargoAllocator.vue</files>
  <action>
    D-01: Chỉnh sửa `CargoAllocator.vue` để bổ sung mục "Lịch sử cân Kho & Container" vào menu sidebar bên trái:
    - Nút bấm cập nhật `activeSubViewMode = 'other_tickets'`.
    - Biển tượng icon dùng Material symbol `history` hoặc tương tự.
    - Cập nhật định nghĩa kiểu dữ liệu cho `activeSubViewMode` nếu có khai báo kiểu tường minh (nếu không thì gán chuỗi trực tiếp).
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>Mục điều hướng mới được thêm vào sidebar và biên dịch TS thành công.</done>
</task>

<task type="auto">
  <name>Task 2: Import WeighbridgeOtherManager component stub</name>
  <files>src/components/tools/CargoAllocator.vue</files>
  <action>
    D-01: Import component `WeighbridgeOtherManager` ở phần `<script setup>` của `CargoAllocator.vue`.
    Thêm khối `<div v-else-if="activeSubViewMode === 'other_tickets'">` trong `<main>` để chứa `<WeighbridgeOtherManager />`.
    (Tạo file WeighbridgeOtherManager.vue tạm dạng stub rỗng trước nếu TypeScript báo lỗi import).
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>Component stub được liên kết điều kiện và build thành công.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Không có (chỉ thay đổi giao diện điều hướng phía client).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | Sidebar Menu | ignore | Giao diện client-side hoàn toàn vô hại. |
</threat_model>

<verification>
vue-tsc kiểm tra biên dịch mã nguồn.
</verification>

<success_criteria>
Menu điều hướng hoạt động tốt và biên dịch không có lỗi TypeScript.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-dashboard-sidebar-navigation/06-01-SUMMARY.md`
</output>
