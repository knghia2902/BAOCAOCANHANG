---
phase: 06-ui-dashboard-sidebar-navigation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/tools/WeighbridgeOtherManager.vue
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "D-01: Tab switcher for Cân Kho and Cân Container is functional"
    - "D-02: Excel drag and drop upload panel is built with preview state"
    - "D-03: Table pagination is implemented defaulting to 20 rows with selector options"
  artifacts:
    - path: "src/components/tools/WeighbridgeOtherManager.vue"
      provides: "Lookup dashboard component for warehouse and container weighing logs"
      contains: "weighbridgeOtherService"
---

<objective>
Xây dựng component `WeighbridgeOtherManager.vue` tích hợp đầy đủ:
1. Giao diện Tab switcher (D-01) chuyển đổi Cân Kho và Cân Container.
2. Drag & Drop zone (D-02) để tải và phân tích file Excel.
3. Bảng kết quả có phân trang (D-03) hiển thị dữ liệu đồng bộ từ Supabase.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-dashboard-sidebar-navigation/06-CONTEXT.md
@.planning/phases/06-ui-dashboard-sidebar-navigation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build WeighbridgeOtherManager template and state</name>
  <files>src/components/tools/WeighbridgeOtherManager.vue</files>
  <action>
    D-01: Xây dựng cấu trúc HTML/CSS và script setup cho component:
    - Khai báo reactive state: `activeTab`, `tickets`, `currentPage`, `itemsPerPage`.
    - Tab bar switcher chuyển qua lại hai tab.
    - Cấu hình layout chính sử dụng các portfolio design class (soft-shadow, teal accents).
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>Giao diện nền tảng và quản lý state của component hoàn thành sạch sẽ.</done>
</task>

<task type="auto">
  <name>Task 2: Build Drag & Drop Zone and Save Action</name>
  <files>src/components/tools/WeighbridgeOtherManager.vue</files>
  <action>
    D-02: Viết giao diện Drag & Drop vùng uploader:
    - Bắt sự kiện dragover, drop và click chọn file.
    - Đọc file dưới dạng ArrayBuffer, gọi `weighbridgeOtherService.parseExcelFile` để trích xuất dữ liệu xem trước (preview).
    - Tạo nút "Lưu dữ liệu" gọi đến dịch vụ lưu của `weighbridgeOtherService` và làm sạch (clear) preview sau khi hoàn tất.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>Tính năng kéo thả tệp Excel và gửi lưu Supabase hoạt động trơn tru.</done>
</task>

<task type="auto">
  <name>Task 3: Build Paginated Records Table</name>
  <files>src/components/tools/WeighbridgeOtherManager.vue</files>
  <action>
    D-03: Thiết lập bảng hiển thị danh sách phiếu cân tải từ Supabase (thông qua getWarehouseTickets / getContainerTickets):
    - Hiển thị đầy đủ thông tin: biển xe, ngày giờ, số phiếu, cân 1/2, khối lượng tịnh.
    - Phân trang dưới cuối bảng (20 dòng/trang) kèm dropdown chọn kích thước (10, 20, 50, 100) và các nút di chuyển trang.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>Bảng dữ liệu phân trang và chuyển đổi tab hiển thị kết quả chính xác.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Excel File -> Client uploader.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Denial of Service | File Uploader | mitigate | Giới hạn dung lượng file Excel tải lên trong component và chặn xử lý nếu vượt ngưỡng. |
</threat_model>

<verification>
vue-tsc kiểm tra kiểu dữ liệu của component và build pass.
</verification>

<success_criteria>
Giao diện tải lên, chuyển tab, và phân trang hoạt động hoàn hảo không gây lỗi TypeScript.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-dashboard-sidebar-navigation/06-02-SUMMARY.md`
</output>
