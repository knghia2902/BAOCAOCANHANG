---
phase: 05-supabase-schema-weighbridgeotherservice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/excel.ts
autonomous: true
requirements:
  - DB-01
  - DB-02

must_haves:
  truths:
    - "D-01: TypeScript interfaces WarehouseTicket and ContainerTicket are defined for the two separate tables"
    - "D-01: SQL scripts for creating weighbridge_warehouse_tickets and weighbridge_container_tickets are prepared"
  artifacts:
    - path: "src/types/excel.ts"
      provides: "TypeScript models for warehouse and container weighing tickets"
      contains: "WarehouseTicket"
    - path: ".planning/phases/05-supabase-schema-weighbridgeotherservice/schema.sql"
      provides: "Supabase PostgreSQL table definitions with constraints"
      contains: "weighbridge_warehouse_tickets"
---

<objective>
D-01: Thiết lập cấu trúc cơ sở dữ liệu trên Supabase (PostgreSQL) bằng cách chuẩn bị mã SQL tạo bảng và định nghĩa các interfaces/types TypeScript tương ứng cho dữ liệu cân kho và container.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-supabase-schema-weighbridgeotherservice/05-CONTEXT.md
@.planning/phases/05-supabase-schema-weighbridgeotherservice/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare Supabase SQL Schema File</name>
  <files>.planning/phases/05-supabase-schema-weighbridgeotherservice/schema.sql</files>
  <action>
    D-01: Tạo tệp SQL chứa định nghĩa bảng `weighbridge_warehouse_tickets` và `weighbridge_container_tickets` với các ràng buộc khóa chính, khóa duy nhất `unique (ticket_no)` để phục vụ cho việc ghi đè và các cột chứa thông tin cân hàng.
  </action>
  <verify>
    <automated>Test SQL file syntax check if possible, or manual inspection</automated>
  </verify>
  <done>Tệp schema.sql được tạo chính xác với cấu trúc bảng riêng biệt.</done>
</task>

<task type="auto">
  <name>Task 2: Define TypeScript Interfaces</name>
  <files>src/types/excel.ts</files>
  <action>
    Mở rộng hoặc cập nhật tệp `src/types/excel.ts` (hoặc tạo mới nếu chưa có) để định nghĩa các interface `WarehouseTicket` và `ContainerTicket` khớp với các cột dữ liệu trong cơ sở dữ liệu.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>Các interface được khai báo thành công và không gây lỗi biên dịch TypeScript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Supabase API | Truy cập trực tiếp cơ sở dữ liệu thông qua API client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Elevation of Privilege | Supabase Tables | mitigate | Đảm bảo bật RLS (Row Level Security) cho các bảng mới tạo và cấu hình policy bảo mật thích hợp. |
</threat_model>

<verification>
Kiểm tra cú pháp SQL và chạy vue-tsc kiểm tra kiểu dữ liệu TypeScript.
</verification>

<success_criteria>
SQL Schema sẵn sàng để thực thi trên Supabase và kiểu dữ liệu TypeScript được tích hợp sạch sẽ vào mã nguồn.
</success_criteria>

<output>
After completion, create `.planning/phases/05-supabase-schema-weighbridgeotherservice/05-01-SUMMARY.md`
</output>
