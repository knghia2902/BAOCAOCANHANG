---
phase: 05-supabase-schema-weighbridgeotherservice
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/services/excel/WeighbridgeOtherService.ts
  - tests/WeighbridgeOtherService.spec.ts
autonomous: true
requirements:
  - IMP-01
  - IMP-02
  - DB-01
  - DB-02

must_haves:
  truths:
    - "WeighbridgeOtherService is implemented with methods for fetching, upserting, and parsing Excel logs"
    - "D-02: Flexible header matching is supported for standard columns"
    - "D-03: Overwrite logic works for existing ticket numbers based on unique constraint"
    - "Upsert chunks records in batches of 100 to avoid API timeouts"
    - "Automated unit tests exist and pass cleanly"
  artifacts:
    - path: "src/services/excel/WeighbridgeOtherService.ts"
      provides: "CRUD methods and Excel parser for warehouse/container weighlists"
      contains: "WeighbridgeOtherService"
    - path: "tests/WeighbridgeOtherService.spec.ts"
      provides: "Unit tests verifying parsing, mapping, and database integrations"
      contains: "describe('WeighbridgeOtherService'"
---

<objective>
Xây dựng lớp dịch vụ `WeighbridgeOtherService.ts` chịu trách nhiệm đọc và phân tích file Excel báo cáo cân hàng, ánh xạ thông minh các tiêu đề cột (D-02), và lưu trữ dữ liệu đồng bộ lên Supabase bằng cơ chế bulk upsert (chia nhỏ thành các chunk 100 dòng) cùng chiến lược ghi đè khi trùng số phiếu (D-03). Viết unit tests tự động để đảm bảo chất lượng code.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-supabase-schema-weighbridgeotherservice/05-CONTEXT.md
@.planning/phases/05-supabase-schema-weighbridgeotherservice/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement WeighbridgeOtherService.ts</name>
  <files>src/services/excel/WeighbridgeOtherService.ts</files>
  <action>
    Tạo tệp `src/services/excel/WeighbridgeOtherService.ts`. Thực hiện import `exceljs` và Supabase client `src/supabase.ts`.
    Viết các hàm:
    - D-02: `parseExcelFile(fileBuffer: ArrayBuffer, type: 'warehouse' | 'container'): Promise<any[]>` với cơ chế khớp tiêu đề cột thông minh, hỗ trợ trích xuất `container_no` cho container và `goods_name` cho kho.
    - D-03: `saveWarehouseTickets(tickets: WarehouseTicket[]): Promise<{success: boolean, error?: any}>` sử dụng bulk upsert với ghi đè khi trùng lặp.
    - D-03: `saveContainerTickets(tickets: ContainerTicket[]): Promise<{success: boolean, error?: any}>` sử dụng bulk upsert với ghi đè khi trùng lặp.
    - `getWarehouseTickets(filters?: any): Promise<WarehouseTicket[]>`
    - `getContainerTickets(filters?: any): Promise<ContainerTicket[]>`
    - `deleteWarehouseTicket(id: number): Promise<boolean>`
    - `deleteContainerTicket(id: number): Promise<boolean>`
    - `clearAllWarehouseTickets(): Promise<boolean>`
    - `clearAllContainerTickets(): Promise<boolean>`
    Đảm bảo việc lưu (save) sử dụng bulk upsert với chunk size = 100 để an toàn cho bộ nhớ và API.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>WeighbridgeOtherService được định nghĩa đầy đủ các chức năng và build thành công.</done>
</task>

<task type="auto">
  <name>Task 2: Write Unit Tests with Vitest</name>
  <files>tests/WeighbridgeOtherService.spec.ts</files>
  <action>
    Tạo tệp test `tests/WeighbridgeOtherService.spec.ts` sử dụng Vitest.
    Mô phỏng (Mock) việc gọi API Supabase và mock cấu trúc worksheet excel để kiểm tra tính đúng đắn của:
    - D-02: Hàm phân tích Excel (parseExcelFile) với các tiêu đề cột khác nhau (Tiếng Việt có dấu, không dấu, chữ hoa, chữ thường).
    - Logic chunking khi lưu dữ liệu.
    - D-03: Logic ghi đè (overwrite/upsert) khi xảy ra trùng lặp số phiếu.
  </action>
  <verify>
    <automated>npx vitest run tests/WeighbridgeOtherService.spec.ts</automated>
  </verify>
  <done>Các unit test chạy và pass 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Excel File Upload -> Memory | Nhập file từ người dùng bên ngoài vào bộ nhớ trình duyệt để phân tích. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Denial of Service | Excel Parser | mitigate | Đặt giới hạn kích thước file tải lên (ví dụ tối đa 15MB) và xử lý lỗi try/catch để tránh treo tab trình duyệt. |
| T-05-02-02 | Tampering | Excel Cells | mitigate | Thực hiện ép kiểu dữ liệu nghiêm ngặt đối với các trường số (weight) và ngày tháng (date) để tránh injection dữ liệu bẩn vào DB. |
</threat_model>

<verification>
Chạy test suite của Vitest và vue-tsc kiểm tra kiểu dữ liệu toàn cục.
</verification>

<success_criteria>
Service biên dịch không lỗi, các hàm nghiệp vụ chính xác, các ca kiểm thử tự động pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-supabase-schema-weighbridgeotherservice/05-02-SUMMARY.md`
</output>
