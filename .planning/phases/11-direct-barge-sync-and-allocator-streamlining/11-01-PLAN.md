---
phase: 11-direct-barge-sync-and-allocator-streamlining
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tools/WeighbridgePrinter.vue
autonomous: true
requirements:
  - SYNC-DIRECT-03

must_haves:
  truths:
    - Báo cáo tổng quan (WeighbridgePrinter) đồng bộ sà lan trực tiếp từ allocator_tickets theo orderNo
    - Các trường thông tin từ phiếu cân import (ticketNo, plateNumber, driverName, weight1, weight2, weightNet, dateIn, dateOut, notes, cargoType, customer, orderNo) được chuyển đổi đầy đủ và chính xác sang model Truck
    - Quá trình đồng bộ sang sà lan hoàn toàn độc lập và không xóa dữ liệu phiếu cân ở Tab 1
    - Nếu sà lan bị khóa (locked: true), hệ thống hiển thị cảnh báo và không cho phép đồng bộ
  artifacts:
    - path: src/components/tools/WeighbridgePrinter.vue
      provides: Direct barge sync logic from allocator_tickets filtered strictly by orderNo
---

<objective>
Cập nhật logic đồng bộ dữ liệu vào Sà lan tại Báo cáo tổng quan (WeighbridgePrinter.vue) để lấy trực tiếp từ danh sách Phiếu cân đã import (allocator_tickets) theo Mã lệnh (orderNo) thay vì qua bảng phân bổ cũ.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-direct-barge-sync-and-allocator-streamlining/11-CONTEXT.md
@.planning/phases/11-direct-barge-sync-and-allocator-streamlining/11-RESEARCH.md
</context>

<tasks>

<task id=11-01-01>
<title>Cập nhật nguồn dữ liệu và mapping trong syncFromAllocator và syncFromAllocatorActiveBarge</title>
<action>
Trong src/components/tools/WeighbridgePrinter.vue:
1. Thay đổi truy vấn lấy danh sách chuyến xe đồng bộ từ settings.allocator_generated_trips sang settings.allocator_tickets (kèm fallback dbContext.get('allocator_tickets')).
2. Trong hàm so khớp isBargeMatch(trip, barge):
   - Đảm bảo so sánh chuẩn 	rip.orderNo với arge.config.orderNo (loại bỏ khoảng trắng, không phân biệt hoa thường).
3. Cập nhật hàm chuyển đổi bản ghi phiếu cân sang đối tượng Truck:
   - 	icketNo: 	.ticketNo || ''
   - sourceTicketNo: 	.ticketNo || ''
   - plateNumber: 	.plateNumber || ''
   - driver: 	.driverName || ''
   - weight1: Number(t.weight1) || 0
   - weight2: Number(t.weight2) || 0
   - weightNet: Number(t.weightNet) || 0
   - dateIn: ghép 	.dateInStr và 	.timeInStr (hoặc giữ nguyên nếu đã là chuỗi ngày giờ hợp lệ)
   - dateOut: ghép 	.dateOutStr và 	.timeOutStr
   - 
ote: 	.notes || ''
   - cargoType: 	.cargoType || ''
   - customer: 	.customer || ''
   - orderNo: 	.orderNo || ''
4. Cập nhật các câu thông báo toast từ Báo cáo phân bổ thành Dữ liệu cân hàng / Phiếu cân import để phản ánh đúng ngữ cảnh mới.
</action>
<verification>Chạy 
pm run build để xác thực không có lỗi TypeScript / Vue compiler.</verification>
</task>

</tasks>
