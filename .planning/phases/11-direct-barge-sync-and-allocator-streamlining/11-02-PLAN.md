---
phase: 11-direct-barge-sync-and-allocator-streamlining
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/components/tools/CargoAllocator.vue
autonomous: true
requirements:
  - SYNC-DIRECT-01
  - SYNC-DIRECT-02
  - SYNC-DIRECT-04
  - SYNC-DIRECT-05

must_haves:
  truths:
    - Giao diện Dữ liệu cân hàng chỉ còn 2 tab: Tab 1 (Phiếu cân) và Tab 2 (Theo dõi), tab Phân bổ đã bị xóa hoàn toàn
    - Các card cấu hình phân bổ (Quy tắc phân bổ, Cấu hình tải trọng xe) đã được loại bỏ
    - Nút 'Lưu vào Sổ theo dõi' được đặt tại thanh công cụ của Tab 1, chuyển phiếu cân vào lịch sử và xóa sạch Tab 1 sau khi lưu
    - Tab 2 (Theo dõi) hiển thị bảng và xuất Excel với đầy đủ 14 cột tiêu chí giống như file gốc import ở Tab 1
  artifacts:
    - path: src/components/tools/CargoAllocator.vue
      provides: Streamlined 2-tab view with Tab 1 direct save and Tab 2 standardized schema
---

<objective>
Tái cấu trúc màn hình Dữ liệu cân hàng (CargoAllocator.vue): loại bỏ Tab Phân bổ và các thẻ cấu hình thừa, chuyển nút Lưu vào Sổ theo dõi về Tab 1 kèm cơ chế dọn dẹp Tab 1 sau khi lưu, chuẩn hóa bảng hiển thị và tính năng xuất Excel của Tab 2 Theo dõi theo cấu trúc file gốc import ở Tab 1.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-direct-barge-sync-and-allocator-streamlining/11-CONTEXT.md
@.planning/phases/11-direct-barge-sync-and-allocator-streamlining/11-RESEARCH.md
</context>

<tasks>

<task id=11-02-01>
<title>Loại bỏ Tab Phân bổ và các card cấu hình Quy tắc phân bổ, Cấu hình tải trọng xe</title>
<action>
Trong src/components/tools/CargoAllocator.vue:
1. Xóa bỏ thẻ Quy tắc phân bổ (chiến lược chia, định thời gian, giãn cách).
2. Xóa bỏ thẻ Cấu hình tải trọng xe (TTTP, giới hạn tải trọng xe).
3. Thu gọn thanh điều hướng tab chỉ còn 2 tab:
   - ctiveDataTab: 'source' | 'generated' (hoặc 'source' | 'history').
   - Nút 1: 1. Phiếu cân ({{ csvRecords.length }})
   - Nút 2: 2. Theo dõi ({{ existingTrips.length }})
4. Xóa bỏ toàn bộ phần template của Tab 2 cũ (Bảng Phân bổ, pagination, modal và các controls liên quan đến phân bổ synthetic trips).
5. Dọn dẹp các biến state và hàm không còn sử dụng (generatedTrips, egenerateAllocatedTrips, v.v.).
</action>
<verification>Kiểm tra template render đúng 2 tab và không còn các controls phân bổ.</verification>
</task>

<task id=11-02-02>
<title>Bổ sung nút 'Lưu vào Sổ theo dõi' tại Tab 1 và logic dọn dẹp sau khi lưu</title>
<action>
Trong src/components/tools/CargoAllocator.vue:
1. Thêm nút Lưu vào Sổ theo dõi (kèm icon và style nổi bật) vào toolbar của Tab 1 (bên cạnh nút Import và Xóa tất cả).
2. Viết hàm saveSourceTicketsToHistory():
   - Kiểm tra nếu csvRecords.length === 0 thì thông báo toast 'Không có phiếu cân nào để lưu!'.
   - Hiển thị dialog xác nhận: 'Bạn có chắc chắn muốn lưu toàn bộ [N] phiếu cân này vào Sổ Theo Dõi? Sau khi lưu thành công, danh sách phiếu cân hiện tại sẽ được dọn dẹp.'
   - Khi xác nhận:
     - Map các phiếu cân trong csvRecords sang định dạng lưu trữ của existingTrips / weighbridge_tracking (thông qua AllocatorService.saveTrips).
     - Lưu thành công -> Xóa sạch csvRecords.value = [], xóa file csvFile.value = null.
     - Cập nhật Supabase settings.allocator_tickets = [] và IndexedDB.
     - Hiển thị toast thành công và chuyển tab sang Tab 2 (ctiveDataTab.value = 'generated') để người dùng xem lại.
</action>
<verification>Kiểm tra luồng lưu và xóa sạch dữ liệu tại Tab 1 qua test logic.</verification>
</task>

<task id=11-02-03>
<title>Chuẩn hóa bảng hiển thị và hàm xuất Excel của Tab 2 (Theo dõi) theo cấu trúc form gốc Tab 1</title>
<action>
Trong src/components/tools/CargoAllocator.vue:
1. Cập nhật bảng của Tab 2 (Theo dõi) để hiển thị 14 cột tiêu chuẩn giống Tab 1:
   - STT, Số phiếu (	icketNo), Mã lệnh (orderNo), Số xe (plateNumber), Khách hàng (customer), Loại hàng (cargoType), TL1 (weight1), TL2 (weight2), Khối lượng hàng (weightNet), Thời gian vào (	imeIn / dateIn), Thời gian ra (	imeOut / dateOut), Tên sà lan (argeName), Tài xế (driverName), Ghi chú (
otes).
2. Cập nhật hàm compileAndDownload():
   - Khi xuất Excel từ Tab 2, thiết lập đúng các cột tiêu chuẩn trên của bảng phiếu cân gốc.
   - Định dạng ô số và ngày giờ rõ ràng, chuyên nghiệp.
</action>
<verification>Chạy 
pm run build để đảm bảo toàn bộ dự án compile thành công không có lỗi.</verification>
</task>

</tasks>
