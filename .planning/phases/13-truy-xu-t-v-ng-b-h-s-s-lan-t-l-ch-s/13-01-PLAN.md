---
phase: 13-truy-xu-t-v-ng-b-h-s-s-lan-t-l-ch-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tools/BargeProfileManager.vue
autonomous: true
requirements:
  - BARGE-SYNC-01
  - BARGE-SYNC-02

must_haves:
  truths:
    - Nút "Truy xuất hồ sơ cũ" (icon history) hiển thị ngay cạnh ô nhập Tên sà lan trong form chỉnh sửa hồ sơ sà lan
    - Khi ô Tên sà lan để trống, bấm nút sẽ hiển thị cảnh báo yêu cầu nhập tên sà lan trước
    - Khi bấm nút, hệ thống quét toàn bộ danh sách sà lan trong các chuyến tàu và khu vực, tìm bản ghi sà lan trùng tên gần nhất (loại trừ sà lan hiện tại)
    - Nếu không tìm thấy bản ghi cũ nào, hiển thị Toast cảnh báo "Không tìm thấy dữ liệu cũ của sà lan [Tên sà lan]!"
    - Nếu tìm thấy, mở hộp thoại xác nhận hiển thị rõ tên sà lan, tên tàu mẹ cũ và ngày của bản ghi gần nhất
    - Khi người dùng xác nhận đồng ý, hệ thống nạp tự động thông số kỹ thuật (trọng tải, công suất), bộ 3 giấy tờ (GCN, Đăng kiểm, Bảo hiểm kèm ảnh đính kèm) và thông tin thuyền bộ (Thuyền trưởng, Máy trưởng, Thủy thủ kèm CCCD, sổ danh bạ và ảnh thuyền viên)
    - Thông tin đặc thù của chuyến hiện tại (Số lệnh, Hàng hóa, Thời gian cập/rời cảng, Cảng cuối, Khai hệ thống, Khóa) được giữ nguyên vẹn không bị ghi đè
    - Các trạng thái hợp lệ của thẻ giấy tờ và thuyền viên tự động cập nhật ngay sau khi nạp dữ liệu
  artifacts:
    - path: src/components/tools/BargeProfileManager.vue
      provides: Nút bấm truy xuất, modal xác nhận đồng bộ và logic tìm kiếm sà lan cũ theo tên
---

<objective>
Bổ sung tính năng "Truy xuất hồ sơ cũ" trong form sà lan (`BargeProfileManager.vue`), cho phép tìm kiếm bản ghi sà lan có cùng tên gần nhất trong lịch sử, mở hộp thoại xác nhận và tự động điền toàn bộ thông số kỹ thuật, giấy tờ (GCN, ĐK, BH), hình ảnh đính kèm và thông tin thuyền bộ sang sà lan mới (giữ nguyên thông tin chuyến hiện tại).
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-truy-xu-t-v-ng-b-h-s-s-lan-t-l-ch-s/13-CONTEXT.md
@.planning/phases/13-truy-xu-t-v-ng-b-h-s-s-lan-t-l-ch-s/13-UI-SPEC.md
@.planning/notes/barge-profile-sync-spec.md
</context>

<tasks>

<task id="13-01-01">
<title>Xây dựng logic tìm kiếm sà lan lịch sử mới nhất theo tên</title>
<action>
Trong `src/components/tools/BargeProfileManager.vue`:
1. Viết hàm `findLatestHistoricalBarge(bargeName: string)`:
   - Chuẩn hóa tên sà lan: `cleanName = bargeName.trim().toUpperCase()`.
   - Nếu `!cleanName`, return `null`.
   - Duyệt qua `vessels.value` và tất cả `v.barges`.
   - Lọc các sà lan thỏa mãn:
     - `b.name.trim().toUpperCase() === cleanName`
     - `b.id !== activeBargeId.value` (loại trừ sà lan hiện tại đang mở).
   - Kiểm tra sà lan có dữ liệu hồ sơ hữu ích (có ít nhất một trong các thông tin: `tonnage`, `hp`, `gcnNo`, `dkNo`, `bhNo`, `captain`, hoặc hình ảnh đính kèm).
   - Sắp xếp danh sách tìm thấy theo thời gian giảm dần: ưu tiên `b.config?.updatedAt`, kế đến `b.created_at` (parse timestamp), hoặc `v.created_at`.
   - Lấy ra bản ghi đầu tiên (mới nhất), trả về đối tượng gồm `{ barge: Barge; vesselName: string; formattedDate: string }`.
2. Tạo reactive state phục vụ modal xác nhận:
   - `showSyncConfirmModal = ref(false)`
   - `syncCandidate = ref<{ barge: Barge; vesselName: string; formattedDate: string } | null>(null)`
   - `isSyncing = ref(false)`
</action>
<verification>Kiểm tra TypeScript biên dịch không có lỗi cú pháp.</verification>
</task>

<task id="13-01-02">
<title>Thêm nút "Truy xuất hồ sơ cũ" trên giao diện và hàm xử lý kích hoạt</title>
<action>
Trong `src/components/tools/BargeProfileManager.vue`:
1. Tại khu vực nhập Tên sà lan (`editBargeName`) trong case B (form chỉnh sửa hồ sơ sà lan):
   - Đặt input và nút bấm trong một flex container:
     - Input `editBargeName` giữ nguyên styling.
     - Ngay bên cạnh (hoặc tích hợp cùng hàng), thêm nút bấm:
       ```html
       <button
           type="button"
           @click="handleTriggerHistorySync"
           class="h-8 px-3 bg-primary/10 hover:bg-primary/20 text-primary border border-primary/20 hover:border-primary/40 font-bold rounded-xl text-xs flex items-center gap-1.5 transition-all active:scale-95 shrink-0"
           title="Truy xuất thông số kỹ thuật, giấy tờ và thuyền viên từ sà lan cũ cùng tên"
       >
           <span class="material-symbols-outlined text-sm">history</span>
           <span class="hidden sm:inline">Truy xuất hồ sơ cũ</span>
       </button>
       ```
2. Viết hàm `handleTriggerHistorySync()`:
   - Kiểm tra nếu `!editBargeName.value.trim()`:
     - Báo Toast `addToast('Vui lòng nhập tên sà lan trước khi truy xuất!', 'info')`.
     - Return.
   - Gọi `findLatestHistoricalBarge(editBargeName.value)`.
   - Nếu không tìm thấy:
     - Báo Toast `addToast(`Không tìm thấy dữ liệu cũ của sà lan ${editBargeName.value.trim().toUpperCase()}!`, 'info')`.
   - Nếu tìm thấy:
     - Gán `syncCandidate.value = candidate`.
     - Mở `showSyncConfirmModal.value = true`.
</action>
<verification>Kiểm tra nút bấm hiển thị đẹp, ăn khớp với design system.</verification>
</task>

<task id="13-01-03">
<title>Tích hợp Modal xác nhận và hàm nạp dữ liệu đồng bộ vào form</title>
<action>
Trong `src/components/tools/BargeProfileManager.vue`:
1. Thêm markup cho Modal xác nhận đồng bộ (`showSyncConfirmModal`):
   - Bố cục backdrop mờ + modal bo tròn `rounded-3xl` đúng chuẩn design system của app (tương tự `showAddBargeModal`).
   - Tiêu đề: "Đồng bộ hồ sơ sà lan cũ" (icon `history`).
   - Nội dung hiển thị rõ ràng:
     - Tên sà lan tìm thấy: `syncCandidate.barge.name`
     - Tàu mẹ / Chuyến: `syncCandidate.vesselName`
     - Thời gian cập nhật: `syncCandidate.formattedDate`
     - Chi tiết các mục sẽ được đồng bộ: Trọng tải & Công suất, Giấy chứng nhận (GCN), Hồ sơ đăng kiểm, Bảo hiểm, Thuyền bộ (Thuyền trưởng, Máy trưởng, Thủy thủ) và toàn bộ hình ảnh đính kèm.
     - Lưu ý: Các thông tin Số lệnh, Hàng hóa, Giờ cập/rời của chuyến hiện tại sẽ được giữ nguyên.
   - Nút hành động: "Hủy bỏ" và "Đồng ý đồng bộ".
2. Viết hàm `applyHistoricalSync()`:
   - Lấy `config = syncCandidate.value.barge.config || {}`.
   - Nạp các thông số kỹ thuật:
     - `editTonnage.value = config.tonnage !== undefined ? config.tonnage : ''`
     - `editHp.value = config.hp !== undefined ? config.hp : ''`
   - Nạp giấy tờ & hình ảnh:
     - GCN: `editGcnNo.value = config.gcnNo || ''`, `editGcnIssuedDate.value = config.gcnIssuedDate || ''`, `editGcnExpiryDate.value = config.gcnExpiryDate || ''`, `editGcnImages.value = Array.isArray(config.gcnImages) ? [...config.gcnImages] : []`
     - ĐK: `editDkNo.value = config.dkNo || ''`, `editDkIssuedDate.value = config.dkIssuedDate || ''`, `editDkExpiryDate.value = config.dkExpiryDate || ''`, `editDkImages.value = Array.isArray(config.dkImages) ? [...config.dkImages] : []`
     - BH: `editBhNo.value = config.bhNo || ''`, `editBhIssuedDate.value = config.bhIssuedDate || ''`, `editBhExpiryDate.value = config.bhExpiryDate || ''`, `editBhImages.value = Array.isArray(config.bhImages) ? [...config.bhImages] : []`
   - Nạp thuyền bộ & hình ảnh:
     - `editCaptain.value = config.captain || ''`
     - `editCaptainGrade.value = config.captainGrade || ''`
     - `editCaptainCccd.value = config.captainCccd || ''`
     - `editChiefEngineer.value = config.chiefEngineer || ''`
     - `editChiefEngineerGrade.value = config.chiefEngineerGrade || ''`
     - `editChiefEngineerCccd.value = config.chiefEngineerCccd || ''`
     - `editSailors.value = config.sailors || ''`
     - `editSailorsCccd.value = config.sailorsCccd || ''`
     - `editHasCrewBook.value = config.hasCrewBook || false`
     - `editCrewImages.value = Array.isArray(config.crewImages) ? [...config.crewImages] : []`
   - Nạp thông tin mở rộng:
     - Nếu `config.customProfileInfo`: chuyển thành mảng `customMetas.value`.
   - Đóng modal: `showSyncConfirmModal.value = false`.
   - Báo Toast thành công: `addToast(`Đã đồng bộ dữ liệu hồ sơ cũ của sà lan ${editBargeName.value}!`, 'success')`.
</action>
<verification>Chạy `npm run build` để kiểm tra toàn bộ ứng dụng biên dịch sạch sẽ không lỗi.</verification>
</task>

</tasks>
