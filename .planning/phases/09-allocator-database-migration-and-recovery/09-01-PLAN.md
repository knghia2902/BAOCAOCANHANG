---
phase: 09-allocator-database-migration-and-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/20260917_create_allocator_history_trips.sql
  - scripts/backup_allocator_history.cjs
  - scripts/migrate_allocator_history.cjs
  - scripts/recover_missing_trips.cjs
  - scripts/verify_parity.cjs
autonomous: true
requirements:
  - DB-MIGRATE-01
  - DATA-RECOVER-01

must_haves:
  truths:
    - "Supabase table allocator_history_trips exists with typed columns and indexes"
    - "Static backup file .planning/backups/allocator_history_trips_backup_16303.json contains all 16,303 original trips"
    - "16,303 historical trips are migrated into allocator_history_trips without data loss"
    - "Preview report for 1,657 missing trips (11/09 - 15/09) is verified and inserted with is_recovered: true"
    - "Parity verification script confirms exactly 17,960 total records in allocator_history_trips"
  artifacts:
    - path: "database/migrations/20260917_create_allocator_history_trips.sql"
      provides: "Postgres DDL for allocator_history_trips table, indexes, and RLS"
      contains: "CREATE TABLE IF NOT EXISTS public.allocator_history_trips"
    - path: "scripts/backup_allocator_history.cjs"
      provides: "Automated static backup script for content.settings"
      contains: "allocator_history_trips_backup_16303.json"
    - path: "scripts/migrate_allocator_history.cjs"
      provides: "Batch migration script inserting 16,303 records in chunks of 500"
      contains: "allocator_history_trips"
    - path: "scripts/recover_missing_trips.cjs"
      provides: "Reconstruction & recovery script for missing trips from 11/09 to 15/09"
      contains: "is_recovered"
    - path: "scripts/verify_parity.cjs"
      provides: "Parity and data integrity verification script"
      contains: "verifyParity"
---

<objective>
Tạo bảng quan hệ `allocator_history_trips` trên Supabase, sao lưu an toàn 16,303 chuyến xe lịch sử ra file tĩnh JSON, thực hiện di chuyển toàn bộ vào bảng mới theo từng đợt (chunks), chạy kịch bản khôi phục 1,657 chuyến xe thiếu từ 11/09 đến 15/09 (có báo cáo xem trước và gắn cờ `is_recovered: true`), và xác minh đối chiếu dữ liệu (parity check) đạt chuẩn 100%.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-allocator-database-migration-and-recovery/09-CONTEXT.md
@.planning/phases/09-allocator-database-migration-and-recovery/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL DDL and provision allocator_history_trips table on Supabase</name>
  <files>database/migrations/20260917_create_allocator_history_trips.sql</files>
  <action>
    - Tạo tệp `database/migrations/20260917_create_allocator_history_trips.sql` chứa toàn bộ câu lệnh DDL theo thiết kế trong 09-RESEARCH.md:
      + Tạo bảng `public.allocator_history_trips` với các cột: `id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY`, `stt INT`, `time_str TEXT`, `plate_number TEXT NOT NULL`, `tttp NUMERIC(10,2)`, `limit_weight NUMERIC(10,2)`, `ticket_no TEXT`, `source_ticket_no TEXT`, `cargo_type TEXT`, `weight_1 NUMERIC(12,2)`, `weight_2 NUMERIC(12,2)`, `weight_net NUMERIC(12,2) NOT NULL`, `weight_tons NUMERIC(10,3) NOT NULL`, `direction TEXT`, `barge_name TEXT`, `order_no TEXT`, `customer TEXT`, `date1_obj TIMESTAMPTZ`, `date2_obj TIMESTAMPTZ`, `notes TEXT`, `is_recovered BOOLEAN NOT NULL DEFAULT FALSE`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`.
      + Tạo các chỉ mục (indexes): `idx_allocator_history_date1`, `idx_allocator_history_plate`, `idx_allocator_history_ticket`, `idx_allocator_history_order`, `idx_allocator_history_customer`, `idx_allocator_history_time_str`, `idx_allocator_history_recovered`.
      + Cấu hình RLS (Row Level Security) cho phép anon key thực hiện SELECT, INSERT, UPDATE, DELETE.
    - Thực thi DDL trên Supabase thông qua script Node.js hoặc Supabase SQL REST API.
    - Kiểm tra bảng đã sẵn sàng bằng lệnh truy vấn test `supabase.from('allocator_history_trips').select('count', { count: 'exact', head: true })`.
  </action>
  <verify>
    <automated>node -e "const { createClient } = require('@supabase/supabase-js'); require('dotenv').config(); const sb = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); sb.from('allocator_history_trips').select('id').limit(1).then(r => { if (r.error) { console.error(r.error); process.exit(1); } console.log('Table allocator_history_trips is ready!'); });"</automated>
  </verify>
  <done>Bảng allocator_history_trips đã được tạo thành công trên Supabase và phản hồi truy vấn sẵn sàng.</done>
</task>

<task type="auto">
  <name>Task 2: Build and execute static JSON backup script for 16,303 trips</name>
  <files>scripts/backup_allocator_history.cjs</files>
  <action>
    - Tạo thư mục `.planning/backups/`.
    - Viết kịch bản `scripts/backup_allocator_history.cjs`:
      + Kết nối Supabase lấy `content.settings.allocator_history_trips`.
      + Xác thực dữ liệu: mảng tồn tại và có độ dài >= 16,000 bản ghi.
      + Ghi ra tệp `.planning/backups/allocator_history_trips_backup_16303.json`.
      + In ra kích thước tệp và mã hash MD5 để làm bằng chứng kiểm toán (audit proof).
    - Chạy script và đảm bảo tệp sao lưu được tạo đầy đủ.
  </action>
  <verify>
    <automated>node scripts/backup_allocator_history.cjs</automated>
  </verify>
  <done>Tệp sao lưu tĩnh 16,303 bản ghi được lưu an toàn tại .planning/backups/allocator_history_trips_backup_16303.json.</done>
</task>

<task type="auto">
  <name>Task 3: Build and execute migration script to populate allocator_history_trips</name>
  <files>scripts/migrate_allocator_history.cjs</files>
  <action>
    - Viết kịch bản `scripts/migrate_allocator_history.cjs`:
      + Đọc 16,303 bản ghi từ tệp backup đã tạo ở Task 2.
      + Chuyển đổi dữ liệu từng chuyến sang định dạng snake_case của bảng `allocator_history_trips`:
        * `limit` -> `limit_weight`
        * `date1Obj` -> `date1_obj` (ISO string)
        * `date2Obj` -> `date2_obj` (ISO string)
        * Gán `is_recovered: false`.
      + Chia nhỏ dữ liệu thành các đợt (chunks) 500 bản ghi để chèn lần lượt lên Supabase, có hiển thị tiến độ (Progress bar).
      + Xử lý retry tự động nếu một đợt gặp lỗi mạng tạm thời.
    - Thực thi migration và xác nhận toàn bộ 16,303 bản ghi đã nằm trong bảng `allocator_history_trips`.
  </action>
  <verify>
    <automated>node scripts/migrate_allocator_history.cjs</automated>
  </verify>
  <done>16,303 bản ghi lịch sử được di chuyển đầy đủ vào bảng Supabase allocator_history_trips.</done>
</task>

<task type="auto">
  <name>Task 4: Build and execute recovery script for 1,657 missing trips (11/09 - 15/09)</name>
  <files>scripts/recover_missing_trips.cjs</files>
  <action>
    - Viết kịch bản `scripts/recover_missing_trips.cjs`:
      + Truy vấn `weighbridge_trucks` trong khoảng thời gian từ `2026-09-11 00:00:00` đến `2026-09-15 23:59:59`.
      + Kết hợp dữ liệu (JOIN) với bảng `weighbridge_barges` qua `barge_id` để lấy tên sà lan, khách hàng (`owner`/`operator`), loại hàng (`goods`), mã lệnh (`orderNo`), chiều hàng (`xn`).
      + Chống trùng lặp: Kiểm tra đối chiếu với `allocator_history_trips` để đảm bảo không chèn lại phiếu cân đã có.
      + Hỗ trợ cờ `--preview`: Xuất bảng tổng hợp số chuyến theo ngày, tổng khối lượng (tấn), danh sách sà lan/khách hàng.
      + Hỗ trợ cờ `--confirm`: Tiến hành INSERT các chuyến xe khôi phục vào `allocator_history_trips` với `is_recovered: true` và `notes: 'Khôi phục từ phiếu cân'`.
    - Chạy script ở chế độ `--preview`, kiểm tra khớp chính xác ~1,657 chuyến, sau đó chạy `--confirm` để hoàn tất khôi phục.
  </action>
  <verify>
    <automated>node scripts/recover_missing_trips.cjs --confirm</automated>
  </verify>
  <done>1,657 chuyến xe thiếu từ 11/09 - 15/09 được khôi phục chuẩn xác vào bảng allocator_history_trips kèm cờ is_recovered.</done>
</task>

<task type="auto">
  <name>Task 5: Parity verification check across all historical and recovered records</name>
  <files>scripts/verify_parity.cjs</files>
  <action>
    - Viết kịch bản kiểm toán `scripts/verify_parity.cjs`:
      + Đếm tổng số dòng trong `allocator_history_trips`: Kỳ vọng đạt đúng ~17,960 dòng (16,303 ban đầu + 1,657 khôi phục).
      + Đếm số lượng `is_recovered = true` (kỳ vọng 1,657) và `is_recovered = false` (kỳ vọng 16,303).
      + So sánh đối chiếu ngẫu nhiên 50 bản ghi giữa bảng mới và tệp backup JSON gốc.
      + Kiểm tra tính liên tục của các ngày trong tháng 9/2026 (đặc biệt các ngày 11, 12, 13, 14, 15/09/2026).
    - Chạy script kiểm tra và in báo cáo kết quả.
  </action>
  <verify>
    <automated>node scripts/verify_parity.cjs</automated>
  </verify>
  <done>Báo cáo đối chiếu xác nhận 100% dữ liệu toàn vẹn, đầy đủ và không bị thất thoát.</done>
</task>

</tasks>

<threat_model>
- Nguy cơ mất mát dữ liệu gốc: Được bảo vệ bởi tệp sao lưu tĩnh .planning/backups/allocator_history_trips_backup_16303.json trước khi thực thi bất kỳ thao tác nào.
- Nguy cơ trùng lặp bản ghi khi khôi phục: Được bảo vệ bởi bộ lọc đối chiếu ticket_no / (plate_number, time_str) trong recover_missing_trips.cjs.
- Nguy cơ timeout PostgREST: Được triệt tiêu bằng cơ chế chia nhỏ thành các đợt 500 bản ghi.
</threat_model>

<reversibility_checkpoints>
- Checkpoint 1: Tệp sao lưu backup_16303.json là căn cứ phục hồi nguyên trạng nếu có lỗi trong quá trình migration.
- Checkpoint 2: Toàn bộ 1,657 chuyến xe khôi phục đều mang cờ is_recovered = true, cho phép xóa/hoàn tác độc lập nếu cần.
</reversibility_checkpoints>
